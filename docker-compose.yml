services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: vintage-house
      POSTGRES_USER: vintage-house
      POSTGRES_PASSWORD: vintage-house
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  
  redis:
    image: redis:7-alpine
    ports:
      - "6480:6379"

  fastapi:
    build: .
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --reload-dir app
    volumes:
      - .:/app
    ports:
      - "8001:8000"
    env_file:
      - env/.env
    depends_on:
      - db
      - redis

volumes:
  postgres_data:
